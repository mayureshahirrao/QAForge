%PDF-1.4
% QAForge sample upload artefact — not a real PDF, intentionally minimal so the
% repository stays text-only. Replace with a genuine PDF in your local checkout
% if downstream services validate file structure.
